{
  "name": "teste-tecnico-tecnotel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm run setup",
    "setup": "npm install --prefix server && npm install --prefix client && npm run prisma:generate && npm run prisma:push",
    "prisma:generate": "npx --prefix server prisma generate",
    "prisma:push": "npx --prefix server prisma db push",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
